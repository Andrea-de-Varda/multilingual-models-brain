bloom_small_multi   = multilingual_encoding(bloom_langs, "bloom_small", 24)
bloom_med_multi     = multilingual_encoding(bloom_langs, "bloom_base", 24)
bloom_xl_multi      = multilingual_encoding(bloom_langs, "bloom_xl", 30)

bloom_xl      = monolingual_encoding(bloom_langs, "bloom_xl", 30, shuffle=False, prefix = "sequential_")


bloom_langs = ["fr", "mr", "es", "ta", "vi"]



#################
# BLOOM - small #
#################

tokenizer = AutoTokenizer.from_pretrained("bigscience/bloom-560m", add_prefix_space=True)
check_special_tokens(tokenizer)
model = AutoModel.from_pretrained("bigscience/bloom-560m")

# ca = embeddings("ca", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
# ja = embeddings("ja", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
# en = embeddings("en", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
es = embeddings("es", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
mr = embeddings("mr", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
# af = embeddings("af", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", split_words = True)
vi = embeddings("vi", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
ta = embeddings("ta", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
# lt = embeddings("lt", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
# tr = embeddings("tr", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
# nl = embeddings("nl", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
#no = embeddings("no", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
#fa = embeddings("fa", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
fr = embeddings("fr", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, special_replace = [('…', '...')], replace=True, split_words = True)
ro = embeddings("ro", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, split_words = True)
# it = embeddings("ita", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_small", alternative_tok = True, special_replace = [('—', '-')], replace=True, split_words = True)

###############
# BLOOM - med #
###############

tokenizer = AutoTokenizer.from_pretrained("bigscience/bloom-1b1", add_prefix_space=True)
check_special_tokens(tokenizer)
model = AutoModel.from_pretrained("bigscience/bloom-1b1")

# ca = embeddings("ca", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
# ja = embeddings("ja", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
# en = embeddings("en", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
es = embeddings("es", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
mr = embeddings("mr", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
# af = embeddings("af", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", split_words = True)
vi = embeddings("vi", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
ta = embeddings("ta", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
# lt = embeddings("lt", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
# tr = embeddings("tr", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
# nl = embeddings("nl", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
#no = embeddings("no", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
#fa = embeddings("fa", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
fr = embeddings("fr", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, special_replace = [('…', '...')], replace=True, split_words = True)
ro = embeddings("ro", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, split_words = True)
# it = embeddings("ita", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_base", alternative_tok = True, special_replace = [('—', '-')], replace=True, split_words = True)


##############
# BLOOM - xl #
##############

tokenizer = AutoTokenizer.from_pretrained("bigscience/bloom-3b", add_prefix_space=True)
check_special_tokens(tokenizer)
model = AutoModel.from_pretrained("bigscience/bloom-3b")

# ca = embeddings("ca", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
# ja = embeddings("ja", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
# en = embeddings("en", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
es = embeddings("es", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
mr = embeddings("mr", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
# af = embeddings("af", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", split_words = True)
vi = embeddings("vi", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
ta = embeddings("ta", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
# lt = embeddings("lt", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
# tr = embeddings("tr", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
# nl = embeddings("nl", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
#no = embeddings("no", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
#fa = embeddings("fa", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
fr = embeddings("fr", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, special_replace = [('…', '...')], replace=True, split_words = True)
ro = embeddings("ro", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, split_words = True)
# it = embeddings("ita", sep = "Ġ", tokenizer = tokenizer, model = model, saveto = "bloom_xl", alternative_tok = True, special_replace = [('—', '-')], replace=True, split_words = True)






#########
# mBART #
#########

tokenizer = AutoTokenizer.from_pretrained("facebook/mbart-large-50")
check_special_tokens(tokenizer)
model = AutoModel.from_pretrained("facebook/mbart-large-50")

# ca = embeddings("ca", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
# ja = embeddings("ja", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
# en = embeddings("en", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
es = embeddings("es", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
mr = embeddings("mr", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
af = embeddings("af", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
vi = embeddings("vi", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
ta = embeddings("ta", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
lt = embeddings("lt", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True, alternative_tok = True)
tr = embeddings("tr", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
nl = embeddings("nl", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True)
# no = embeddings("no", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True, alternative_tok = True)
fa = embeddings("fa", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True, alternative_tok = True)
fr = embeddings("fr", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", special_replace = [('…', '...')], replace=True, emb_start = 1, emb_end = -1, is_seq2seq = True, alternative_tok = True)
ro = embeddings("ro", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", emb_start = 1, emb_end = -1, is_seq2seq = True, alternative_tok = True)
# it = embeddings("ita", sep = "▁", tokenizer = tokenizer, model = model, saveto = "mbart", special_replace = [('—', '-')], replace=True, emb_start = 1, emb_end = -1, is_seq2seq = True)
